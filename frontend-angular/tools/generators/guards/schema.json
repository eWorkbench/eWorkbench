{
  "$schema": "http://json-schema.org/schema",
  "id": "guards",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the guard file, relative to the current workspace. Default is a folder with the same name as the guard in the project root.",
      "visible": false
    },
    "name": {
      "type": "string",
      "description": "Guard name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for this guard?"
    },
    "project": {
      "type": "string",
      "description": "Project to add the guard to"
    },
    "module": {
      "type": "string",
      "description": "The declaring NgModule.",
      "alias": "m"
    },
    "type": {
      "type": "string",
      "description": "Adds a developer-defined type to the filename, in the format \"name.type.ts\".",
      "default": "Guard"
    }
  },
  "required": ["name"]
}
