{
  "$schema": "http://json-schema.org/schema",
  "id": "libraries",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the library file, relative to the current workspace. Default is a folder with the same name as the library in the project root.",
      "visible": false
    },
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for this library?"
    }
  },
  "required": ["name"]
}
